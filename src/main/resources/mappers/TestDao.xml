<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.account.ABook.dao.TestDao">
    <select id="main" resultType="com.account.ABook.model.Member">

        SELECT
            MEMBER_NPM as memberNpm, id, pwd, HIREDATE
        FROM MEMBER
    </select>










</mapper>